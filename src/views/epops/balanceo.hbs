<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/estilos_datos.css">
    <title>Balanceo de Carga</title>
    <style>
        .loading {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 24px;
            color: #000;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="titulo">Balanceo de Carga</h1>
        <form id="balanceo-form" class="form">
            
            <label for="enlace">Seleccione un enlace:</label>
            <div class="select-container">
                <select name="enlace" id="enlace" required class="form-control">
                    {{#each datos.datos_web as |valor clave|}}
                        <option value="{{clave}}">{{valor}}</option>
                    {{/each}}
                </select>
                <span class="select-arrow"></span>
            </div>

            <label for="modoSTP">Modo STP</label>
            <div class="select-container">
                <select id="modoSTP" name="modoSTP" required class="form-control">
                    <option value="">Seleccione...</option>
                    <option value="mstp">MSTP</option>
                </select>
                <span class="select-arrow"></span>
            </div>

            <label for="vlan1">Vlan</label>
            <input type="text" id="vlan1" name="vlan1" placeholder="Rango de VLANs que desea permitir que pase en el enlace no bloqueado">

            <label for="user">Usuario</label>
            <input type="text" id="user" name="user" placeholder="Usuario" required>
            
            <label for="password">Contraseña</label>
            <input type="password" id="password" name="password" placeholder="Contraseña" required>

            <button type="submit" class="btn">Configurar</button>
        </form>
        <div id="message-container" class="message-container"></div>
    </div>

    <script src="/js/balanceo.js"></script>
</body>
</html>
